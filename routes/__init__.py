"""Flask blueprints, auto-discovered by app.py.

Each module in this package that defines a module-level `bp` (a
flask.Blueprint) is registered automatically at startup — drop a new
module in this directory and restart the app.
"""
