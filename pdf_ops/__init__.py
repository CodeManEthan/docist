"""PDF manipulation operations (merge, page tools, watermark, security).

Each module holds pure PDF-processing functions with no Flask dependency,
so they can be unit-tested directly and reused across routes.
"""
